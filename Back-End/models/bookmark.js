// var mongoose = require('mongoose');

// const Schema = mongoose.Schema; //to create a new schema i.e db field 

// const bookmarkSchema = new Schema({ 
//     by:{
//         type: Schema.Types.ObjectId, //for refrencing the person who created it 
//         required:true,
//         ref:'Courses'
//     },
//     course{

//     }
// });

// module.exports = courses = mongoose.model('bookmarks',bookmarkSchema); //stored in users collection  and uses user schema